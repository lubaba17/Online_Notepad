.wrapper_with_padding
  #notes.clearfix
    - unless @notes.blank?
      - @notes.each do |note|
        %a{ href: (url_for [note])}
          .notes
            %p.title= note.title
            %p.date= time_ago_in_words(note.created_at)
    - else
      = link_to image_tag('notey.jpg'), root_path, class: "notey"
      %h2 Add a note!
      %p It appears you haven't created any notes yet... Let's fix that. Why don't you create a new note.
      //%button= link_to "New Note", new_note_path        
 